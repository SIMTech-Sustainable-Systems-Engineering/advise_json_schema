{
  "$id": "https://your-domain.com/schemas/PACT_methodology/DataModelExtension/activityName/schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "PACT Activity Name Extension Schema",
  "description": "A Pathfinder Data Model Extension to specify the datapoint name or name of the process used to generate the product.",
  "type": "object",
  "required": [
    "activityName"
  ],
  "properties": {
    "activityName": {
      "type": "string",
      "minLength": 1,
      "description": "Datapoint name or name of process to generate the product"
    }
  },
  "additionalProperties": false
}
